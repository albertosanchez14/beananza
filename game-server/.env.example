# Environment Variables
SERVER_HOST=localhost
SERVER_PORT=8080

# Redis Configuration
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Logging
LOG_LEVEL=info

# WebSocket Configuration
READ_BUFFER_SIZE=1024
WRITE_BUFFER_SIZE=1024
# Per-client outbound message buffer (number of messages); default 256
WS_SEND_BUFFER_SIZE=256
# Token-bucket rate limiter: sustained messages/sec and burst size
WS_RATE_LIMIT=10
WS_RATE_BURST=30

# CORS — comma-separated list of allowed WebSocket origins
# Leave empty to allow all origins (not recommended for production)
ALLOWED_ORIGINS=https://example.com

# Game Rules
# Number of cards flipped face-up from the draw pile each "turn over beans" phase
CARDS_PER_TURN=2

# Player limits
MAX_NUMBER_PLAYERS=5
MIN_NUMBER_PLAYERS=3

# Path to the cards YAML config file
CARDS_CONFIG_PATH=cards.yaml
