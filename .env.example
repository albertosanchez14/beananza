# Hostname or IP used to build the client URLs.
# Default: localhost (only works from this machine).
# For LAN/multi-browser testing, set this to your machine's local IP,
HOST=localhost

# Host port that nginx binds to. Change if 80 is already in use.
PORT=80
